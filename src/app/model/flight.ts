import { City } from "./city";

export class Flight {
  // Id: number;
  // DepartureStation: City;
  // DepartureStationId: number;
  // ArrivalStation: City;
  // ArrivalStationId: number;
  // DepartureDate: Date;
  // FlightNumber: string;
  // Price: number;
  // Currency: string;
  // DurationHour: number;
}
